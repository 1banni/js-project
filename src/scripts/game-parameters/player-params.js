

export const PLAYER = {
  MAX_HEALTH: 100,
  MAX_SPEED: 4, // PROOF this isn't hitting cause setVelo isn't complete
  RADIUS: 20,
  TURN_RADIUS: 0.5,
  ACCELERATION: 0.01,
  PROJECTILES: 100*100*100,
  // MAX_NOS: 40,
  // TURN_RADIUS: 3;
}
